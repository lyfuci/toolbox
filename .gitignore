# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript incremental build cache
*.tsbuildinfo

# Local env files (whitelist .env.example if/when introduced)
.env
.env.*
!.env.example

# Test coverage
coverage

# Claude Code session artifacts
.claude/

# design-sync (claude.ai/design) — staged scripts, build output, machine state.
# The durable set under .design-sync/ (config.json, NOTES.md, conventions.md,
# previews/, overrides/) IS committed; everything below is regenerated.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# design-sync PixelForge kit (second DS) — regenerated bundle
pf-bundle/
